{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo root for the portfolio platform (server/ + client/).",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx,js,mjs,json,md,css}": "prettier --write"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3"
  }
}
